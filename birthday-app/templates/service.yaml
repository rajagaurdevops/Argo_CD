apiVersion: v1
kind: Service
metadata:
  name: birthday-app
spec:
  type: {{ .Values.service.type }}
  selector:
    app: birthday-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
